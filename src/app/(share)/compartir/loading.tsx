import { CompartirScreen } from "./CompartirScreen";

/** El esqueleto tiene la forma de lo que viene, no un spinner girando. */
export default function Loading() {
  return <CompartirScreen registro={null} previewUrl={null} />;
}
